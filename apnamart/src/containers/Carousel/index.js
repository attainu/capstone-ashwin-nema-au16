import { useSelector } from "react-redux";
import Carousel from 'react-elastic-carousel'
const Allitemsscrollbar = () => {
    // const Allitemscount = useSelector(state => state.Itemslist)
    // const Allprices = useSelector(state => state.Priceslist)
    const breakpoints = [
        {width:3000, itemsToShow: 5}
    ]

    const Allitems = useSelector(state => state.)

    return (
        <div className="row">

        </div>
    )
}

export default Allitemsscrollbar